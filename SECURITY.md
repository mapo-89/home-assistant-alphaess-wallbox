# Security policy

Do not include passwords, access tokens, refresh tokens, session responses or system serial numbers in public issues or logs.

Please use GitHub private vulnerability reporting for security issues. If a credential has been exposed, rotate it at AlphaESS immediately and reconfigure the integration afterwards.

The integration stores credentials in the Home Assistant config entry and keeps session tokens internal. Protect Home Assistant backups accordingly.

